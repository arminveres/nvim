return {
    settings = {
        texlab = {
            inlayHints = {
                -- WARN: (aver) these options do not work for some reason.
                -- citations = false,
                labelDefinitions = false,
                labelReferences = false,
            },
            formatterLineLength = 120,
        },
    },
}
